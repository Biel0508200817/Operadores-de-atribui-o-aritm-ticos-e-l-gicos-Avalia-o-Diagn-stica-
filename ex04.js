/**
 * Crie uma variável `count` com o valor `5`.

Utilize os operadores `++` e `--` para:

- Incrementar `count` e exibir o resultado
- Decrementar `count` e exibir o resultado
 */

var count = 5;

count++;
console.log("Incrementar: ", count);

count--;
console.log("Decrementar: ", count);