/**
 * Dadas as variáveis:

```jsx
let p = true;
let q = false;
```

Determine e exiba o resultado das seguintes expressões:

- `p && q`
- `p || q`
- `!p`
- `!q`
 */

let p = true;
let q = false;

console.log("p && q: ", p && q);
console.log("p || q: ", p || q);
console.log("!p: ", !p);
console.log("!q: ", !q);